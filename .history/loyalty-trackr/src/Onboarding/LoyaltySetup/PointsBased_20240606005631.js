import React, { useState } from "react";

const PointsBased = () => {
  const [pointsPerPurchase, setPointsPerPurchase] = useState(null);
  const [redemptionOptions, setRedemptionOptions] = useState(null);
  const [expirationPeriod, setExpirationPeriod] = usestate(null);
  const [maximumPointsPerPurchase, setMaximumPointsPerPurchase] =
    useState(null);
};
